const endpoints = {
  // Auth
  POST_AUTH_EMAIL: '/auth',
  POST_REGISTER_EMAIL: '/auth/register',

  // User
  GET_USER_ME: '/users/me',

  // Post
  POSTS: '/posts',

  // Translations
  TRANSLATIONS: '/translations',
};

export default endpoints;
